# Voici la copie de la requete sql que j'ai faite depuis le site stackExchange afin de récupérer mes données

'''SELECT Title, Body, Tags
FROM Posts
WHERE PostTypeID = 1
  AND Id < 50000
  AND Score = 3;'''
